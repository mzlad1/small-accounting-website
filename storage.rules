rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Receipts archive, property photos, backups: signed-in users only.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
