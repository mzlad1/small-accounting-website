rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-tenant business app: any signed-in user (the owner's
    // accounts) may read/write; everyone else is locked out.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
